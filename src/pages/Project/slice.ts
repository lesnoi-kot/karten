import { createSlice } from "@reduxjs/toolkit";

export const {
  actions,
  reducer,
  name: sliceName,
} = createSlice({
  name: "project",
  initialState: {},
  reducers: {},
  extraReducers: (builder) => {},
});
